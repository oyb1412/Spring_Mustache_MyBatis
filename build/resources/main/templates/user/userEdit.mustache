{{> other/header}}

<main class="container mx-auto px-4 pt-24 pb-12">
  <div class="flex flex-col md:flex-row gap-8">

    <!-- 왼쪽 메인 콘텐츠 영역 -->
    <div class="w-full md:w-3/4">
      <div class="bg-white rounded-lg shadow-sm p-6 border border-gray-100 space-y-6">
        <h1 class="text-2xl font-bold text-gray-800">🔧 개인정보 수정</h1>

        <!-- 입력 영역 -->
        <div id="user-edit-form" class="space-y-6">

          <!-- 아이디 (readonly) -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">아이디 (변경 불가)</label>
            <input type="text" id="username" value="{{user.username}}" readonly class="w-full px-3 py-2 border rounded bg-gray-100 text-gray-700" />
          </div>

          <!-- 현재 닉네임 (readonly) -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">현재 닉네임</label>
            <input type="text" id="currentNickname" value="{{user.nickname}}" readonly class="w-full px-3 py-2 border rounded bg-gray-100 text-gray-700" />
          </div>

          <!-- 새 닉네임 -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">새 닉네임</label>
            <input type="text" id="newNickname" placeholder="변경할 닉네임 입력" class="w-full px-3 py-2 border rounded" />
          </div>

          <!-- 새 비밀번호 -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">새 비밀번호</label>
            <input type="password" id="newPassword" placeholder="새 비밀번호" class="w-full px-3 py-2 border rounded" />
          </div>

          <!-- 비밀번호 확인 -->
          <div>
            <label class="block text-sm font-medium text-gray-700 mb-1">비밀번호 확인</label>
            <input type="password" id="confirmPassword" placeholder="비밀번호 재입력" class="w-full px-3 py-2 border rounded" />
          </div>

          <!-- 버튼 -->
          <div class="pt-2 flex justify-end gap-2">
            <a href="/my-page" class="bg-gray-400 text-white px-4 py-2 rounded hover:bg-gray-500 text-sm">취소</a>
            <button id="btn-edit" class="bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600 text-sm">수정하기</button>
          </div>
        </div>

        <!-- 안내 -->
        <div class="text-xs text-gray-400 border-t pt-4">
          ⚠️ 닉네임은 중복 불가 / 비밀번호는 5자 이상으로 설정해주세요.
        </div>
      </div>
    </div>

    <!-- 오른쪽 사이드바 -->
    {{> other/sidebar}}

  </div>
</main>

{{> other/footer}}
<script src="/js/userEdit.js"></script>